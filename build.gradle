apply plugin: 'groovy'
apply plugin: 'maven-publish'

group 'kz.cloudsolution.gradle.plugins'
version '1.0.1'

publishing {
    publications {
        maven(MavenPublication) {
            groupId group
            artifactId project.name
            version version

            from components.java
        }
    }
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}